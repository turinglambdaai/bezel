// <groupbox>_gen.cpp — generated by tools/generator; edit the spec instead.

#include "../internal.h"

#include <QGroupBox>

namespace {

using namespace bezel;

QGroupBox* resolve_obj(bezel_handle h, const char* what) {
    return resolve_as<QGroupBox>(h, what);
}

QWidget* resolve_parent(bezel_handle h, const char* what) {
    return h ? resolve_as<QWidget>(h, what) : nullptr;
}

}  // namespace

using namespace bezel;

extern "C" {

BEZEL_EXPORT bezel_handle groupbox_new(const char* in0, bezel_handle in1) {
    return on_gui([=]() -> bezel_handle {
        const QString q_str0 = QString::fromUtf8(in0 ? in0 : "");
        QWidget* parent1 = resolve_parent(in1, "groupbox_new");
        if (in1 && !parent1) return nullptr;
        return register_object(new QGroupBox(q_str0, parent1));
    });
}

BEZEL_EXPORT int groupbox_set_title(bezel_handle h, const char* in0) {
    return on_gui([=]() -> int {
        QGroupBox* obj = resolve_obj(h, "groupbox_set_title");
        if (!obj) return 0;
        const QString q_str0 = QString::fromUtf8(in0 ? in0 : "");
        obj->setTitle(q_str0);
        return 1;
    });
}

BEZEL_EXPORT const char* groupbox_title(bezel_handle h) {
    return on_gui([=]() -> const char* {
        QGroupBox* obj = resolve_obj(h, "groupbox_title");
        if (!obj) return nullptr;
        return strdup_q(obj->title());
    });
}

BEZEL_EXPORT int groupbox_set_checkable(bezel_handle h, int in0) {
    return on_gui([=]() -> int {
        QGroupBox* obj = resolve_obj(h, "groupbox_set_checkable");
        if (!obj) return 0;
        obj->setCheckable((in0 != 0));
        return 1;
    });
}

BEZEL_EXPORT int groupbox_is_checkable(bezel_handle h) {
    return on_gui([=]() -> int {
        QGroupBox* obj = resolve_obj(h, "groupbox_is_checkable");
        if (!obj) return -1;
        return obj->isCheckable() ? 1 : 0;
    });
}

}  // extern "C"


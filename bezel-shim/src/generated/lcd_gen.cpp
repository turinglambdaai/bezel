// <lcd>_gen.cpp — generated by tools/generator; edit the spec instead.

#include "../internal.h"

#include <QLCDNumber>

namespace {

using namespace bezel;

QLCDNumber* resolve_obj(bezel_handle h, const char* what) {
    return resolve_as<QLCDNumber>(h, what);
}

QWidget* resolve_parent(bezel_handle h, const char* what) {
    return h ? resolve_as<QWidget>(h, what) : nullptr;
}

}  // namespace

using namespace bezel;

extern "C" {

BEZEL_EXPORT bezel_handle lcd_new(bezel_handle in0) {
    return on_gui([=]() -> bezel_handle {
        QWidget* parent0 = resolve_parent(in0, "lcd_new");
        if (in0 && !parent0) return nullptr;
        return register_object(new QLCDNumber(parent0));
    });
}

BEZEL_EXPORT int lcd_display(bezel_handle h, int in0) {
    return on_gui([=]() -> int {
        QLCDNumber* obj = resolve_obj(h, "lcd_display");
        if (!obj) return 0;
        obj->display(in0);
        return 1;
    });
}

BEZEL_EXPORT double lcd_value(bezel_handle h) {
    return on_gui([=]() -> double {
        QLCDNumber* obj = resolve_obj(h, "lcd_value");
        if (!obj) return -1.0;
        return obj->value();
    });
}

BEZEL_EXPORT int lcd_set_digit_count(bezel_handle h, int in0) {
    return on_gui([=]() -> int {
        QLCDNumber* obj = resolve_obj(h, "lcd_set_digit_count");
        if (!obj) return 0;
        obj->setDigitCount(in0);
        return 1;
    });
}

}  // extern "C"

